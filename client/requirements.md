## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel
@hookform/resolvers | Form validation integration with Zod
react-hook-form | Form state management

## Notes
The contact form submits to POST /api/messages.
Using native CSS smooth scrolling for anchor links to sections.
Assuming all standard shadcn components (Button, Input, Textarea, Form, Card, Badge, Toast) are available in @/components/ui/ based on the provided directory structure.
